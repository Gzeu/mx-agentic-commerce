{
  "name": "mx-agentic-commerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "contracts:deploy": "npx tsx scripts/deploy.ts"
  },
  "devDependencies": {
    "tsx": "^4.7.1"
  }
}
